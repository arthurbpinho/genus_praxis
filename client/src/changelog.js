// ⚠ APOSENTADO (demanda #12). As "atualizações do sistema" agora vêm do SERVIDOR — são os
// anúncios do tipo `update` que o admin publica (ver server/index.js e AdminAnnouncements).
// Este arquivo era hardcoded e mostrava notas de desenvolvimento que não deviam aparecer
// para os usuários reais. Mantido vazio só para não quebrar um import esquecido.
export const CHANGELOG = [];
export const LATEST_UPDATE = null;
